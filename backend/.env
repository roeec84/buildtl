# ============================================================================
# APPLICATION SETTINGS
# ============================================================================
# DEBUG: Enable/disable debug mode (shows detailed error messages)
#        Set to False in production for security
DEBUG=True

# ENVIRONMENT: Current environment (development, staging, production)
#              Affects logging, error handling, and other behaviors
ENVIRONMENT=development


# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================
# DATABASE_URL: Connection string for the database
#
# Format for PostgreSQL:
#   postgresql+asyncpg://username:password@host:port/database_name
#
# Format for SQLite (for local development only):
#   sqlite+aiosqlite:///./chatbot.db
#
# asyncpg = Async PostgreSQL driver (faster, recommended for production)
# aiosqlite = Async SQLite driver (good for local development/testing)
#
# Docker uses: postgresql+asyncpg://chatbot_user:chatbot_password@postgres:5432/chatbot_db
DATABASE_URL=postgresql+asyncpg://chatbot_user:chatbot_password@localhost:5432/chatbot_db


# ============================================================================
# SECURITY & AUTHENTICATION
# ============================================================================
# SECRET_KEY: Used to sign JWT tokens for authentication
#             CRITICAL: Change this in production! Generate with:
#             openssl rand -hex 32
#             Keep this secret - anyone with this key can forge authentication tokens
SECRET_KEY=09d25e094faa6ca2556c818166b7a9563b93f7099f6f0f4caa6cf63b88e8d3e7

# ALGORITHM: Algorithm used for JWT token encryption
#            HS256 is standard and secure for most applications
ALGORITHM=HS256

# ACCESS_TOKEN_EXPIRE_MINUTES: How long authentication tokens remain valid
#                               10080 minutes = 7 days
#                               After expiration, users must log in again
ACCESS_TOKEN_EXPIRE_MINUTES=10080


# ============================================================================
# CORS (Cross-Origin Resource Sharing)
# ============================================================================
# BACKEND_CORS_ORIGINS: List of allowed frontend URLs that can call the API
#                       Required for browser security when frontend and backend
#                       are on different ports/domains
#
# Common ports:
#   5173 = Vite dev server (default)
#   5174 = Vite dev server (alternative)
#   3000 = Create React App / Next.js
BACKEND_CORS_ORIGINS=["http://localhost:5173","http://localhost:5174","http://localhost:3000"]


# ============================================================================
# AI/LLM API KEYS
# ============================================================================
# OPENAI_API_KEY: API key for OpenAI models (GPT-4, GPT-3.5, etc.)
#                 Get from: https://platform.openai.com/api-keys
#                 Required for: Using OpenAI models in the chat
OPENAI_API_KEY=

# ANTHROPIC_API_KEY: API key for Anthropic Claude models
#                    Get from: https://console.anthropic.com/
#                    Required for: Using Claude models in the chat
ANTHROPIC_API_KEY=


# ============================================================================
# VECTOR STORE - ChromaDB
# ============================================================================
# ChromaDB is used for semantic search and RAG (Retrieval Augmented Generation)
# It stores document embeddings for intelligent context retrieval
#
# CHROMA_HOST: Hostname where ChromaDB is running
#              localhost = running locally
#              chromadb = Docker container name (when using docker-compose)
CHROMA_HOST=localhost

# CHROMA_PORT: Port where ChromaDB API is accessible
#              8001 when using docker-compose (to avoid conflict with backend on 8000)
#              8000 when running ChromaDB locally
CHROMA_PORT=8001
